language: node_js

node_js:
  - 0.8

before_script:
    - sudo apt-get install apache2
    - sudo service apache2 restart

script:
    - mv jianling/GMU /etc/apache2/sites-available/GMU
    - phantomjs run.js http://localhost/GMU/test.html